<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is used only to serve AdMob banner ads (added 2026-08-17 at the user's
      request, reversing the original offline-only decision). All training data itself
      still never leaves the device — this permission is not used for sync or telemetry.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ViraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">

        <!--
          TEST App ID (Google's published constant, safe to ship in debug/dev builds —
          always serves clearly-labelled test ads, never real ones). Must be replaced
          with the real AdMob App ID from the developer's own AdMob console before any
          release build; swapping it is a release-checklist item, not done here.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          specialUse (not shortService): rest duration is user-configurable (global default
          plus per-exercise override, adjustable +/-15s while running) and can exceed
          shortService's fixed ~3-minute system timeout, which would kill a long rest mid-countdown.
        -->
        <service
            android:name=".service.resttimer.RestTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Rest timer between workout sets, started by the user from an active workout session" />
        </service>
    </application>

</manifest>
