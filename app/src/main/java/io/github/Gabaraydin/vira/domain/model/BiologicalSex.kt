package io.github.Gabaraydin.vira.domain.model

// Picks which US Navy body-fat formula variant applies, and whether the entry form needs
// a hip measurement. Not gender identity — purely the anatomical input the formula needs.
enum class BiologicalSex { MALE, FEMALE }
